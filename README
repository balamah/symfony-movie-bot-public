symfony-movie-bot-public - my telegram bot for movies
=====================================================
a bot that uses IMDB api to search movies

Requirements
------------
To make bot working, install docker

Building
--------
1. Copy ".env.example" to ".env" and configure
2. Configure "config/packages/telegram_bot.yaml"
3. Run command

   make build composer-install start

Running the bot
---------------
Enter into container and run command

  bin/console telegram:polling:start

Or, if you use emacs and my plugin that i didn't release yet, load my
project configuration using "C-c p f l" and press S-<F10>
